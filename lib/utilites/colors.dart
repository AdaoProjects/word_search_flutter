import 'dart:ui';

/// Util class with the game colors.
class GameColors {
  GameColors._();

  /// Primary color.
  static const primary = Color(0xFFA80054);

  /// The color used to draw Play button background.
  static const button_Background = Color(0xFFFE7C00);

  /// The color used to draw Play button background.
  static const button_Background_Light = Color(0xFF00a8a8);

  /// The color used in the rest of buttons, also used in some decoration.
  static const button_Background_Play = Color(0xFF00a8a8);

  /// The color used when the buttons is pressed.
  static const button_Background_Play_Light = Color(0xFFFE7C00);


}